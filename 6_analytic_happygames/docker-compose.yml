services:
  pg_analytic:
    image: postgres:16
    container_name: pg_analytic
    environment:
      POSTGRES_PASSWORD: pass
    ports:
      - "5435:5432"
    volumes:
      - ./setup.sql:/docker-entrypoint-initdb.d/setup.sql