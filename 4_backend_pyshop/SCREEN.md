# Скриншоты выполненного задания (PyShop)

### 1. Настройка принтеров в админ-панели
На этом скриншоте отображены созданные принтеры для точки (point_id: 1) с разделением на типы `kitchen` и `client`.
![Принтеры](./screenshots/1_printers_admin.png)

### 2. Создание заказа через API (Status OK)
Результат POST-запроса к `/api/create_order/`. Сервис успешно принял данные, создал записи в БД и поставил задачи в очередь.
![API Response](./screenshots/2_api_ok.png)

### 3. Сгенерированные PDF в иерархии проекта
Скриншот папки `media/pdf/` и админ-панели чеков. Видно, что воркер успешно обработал задачи, и файлы появились в файловой системе с корректными именами (`<id>_<type>.pdf`).
![Файлы PDF](./screenshots/3_pdf_hierarchy.png)